# Copy this file to .env.local (or .env) and fill in your Supabase credentials.
# Values without defaults are required for authentication and admin uploads.

VITE_SUPABASE_URL=https://erixoghrziqcbamahocs.supabase.co
VITE_SUPABASE_ANON=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVyaXhvZ2hyemlxY2JhbWFob2NzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTYyNzExNzYsImV4cCI6MjA3MTg0NzE3Nn0.Lak0VW5uzkEStS8RpVDuZ2Q8NirHzJv1O5ZWjHtwOQI

# Storage bucket that holds gallery and shop assets. Keep this in sync with your
# Supabase bucket name (defaults to drops-public in the app).
VITE_SUPABASE_BUCKET=drops-public

# Email address that is allowed to access the admin workspace.
VITE_ADMIN_EMAIL=admin@example.com

# Optional legacy support (falls back to this if VITE_ADMIN_EMAIL is missing).
VITE_ALLOWED_EMAIL=admin@example.com

# Optional override for the header logo.
# VITE_LOGO_SRC=https://cdn.yoursite.com/logo.svg

# Endpoint the frontend will call to start a live GCash checkout session.
VITE_GCASH_CHECKOUT_ENDPOINT=/api/gcash/create-payment

# Backend-only secrets for the GCash Pay API or your chosen aggregator.
# Do NOT expose these in the browser build.
GCASH_PUBLIC_KEY=
GCASH_SECRET_KEY=
GCASH_WEBHOOK_SECRET=
GCASH_RETURN_URL=https://yourdomain.com/checkout/complete
